import Component from '@glimmer/component';

export default class PropertyDetailComponent extends Component {
  constructor() {
    super(...arguments);

    // // debugger;
    // dataLayer.push({
    //   custom: {
    //     property: true,
    //     name: this.args.model.name,
    //     path: this.args.model.path
    //   }
    // });
  }
}
